kind: ConfigMap 
apiVersion: v1 
metadata:
  name: env-configmap
data:
  # Configuration values can be set as key-value properties
 POSTGRES_HOST: udagramdb.c1x3h05fehsw.us-east-2.rds.amazonaws.com
 POSTGRES_DB: udagramdb
 AWS_BUCKET: arn:aws:s3:::myudagram-bucket
 AWS_REGION: us-east-2
 AWS_PROFILE: default
 JWT_SECRET: testing
 URL: http://localhost:8100
